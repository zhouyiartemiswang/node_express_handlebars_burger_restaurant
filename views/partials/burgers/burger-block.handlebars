<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{isDevoured}}">
		{{#if isDevoured}}Gone!{{else}}Devour it!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
